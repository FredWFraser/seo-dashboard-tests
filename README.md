# Selenium Automation for SEO Dashboard

## Overview
This repository contains Selenium tests for an SEO dashboard.  
Tests are end-to-end and validate both positive and negative user flows.

## Requirements
- Java 11+
- Maven
- Chrome browser + ChromeDriver
- Optional: `.env` file for configuration

## Configuration
1. Copy the example env file:
   ```bash
   cp .env.example .env